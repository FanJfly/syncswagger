{
  "name": "syncswagger",
  "version": "0.0.1",
  "description": "sync swagger-api to mock server",
  "main": "index.js",
  "bin": {
    "syncSwagger": "bin/syncSwagger.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jiangf",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.0"
  }
}
